<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @grid.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @grid.description %>
</p>

<%= link_to 'Edit', edit_schema_grid_path(@schema,@grid) %> |
<%= link_to 'Back', schema_grids_path(@schema) %>
